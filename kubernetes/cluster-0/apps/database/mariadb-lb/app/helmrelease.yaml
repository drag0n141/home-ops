---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mariadb-lb
  namespace: database
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.2.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controller:
      replicas: 2
      strategy: RollingUpdate
      annotations:
        reloader.stakater.com/auto: "true"
    image:
      repository: public.ecr.aws/docker/library/haproxy
      tag: 2.8.3-alpine@sha256:59fe2aa6e218467a85cb71e565ef3c0be3cbe6927ae2191e59906b878562d950
    service:
      main:
        enabled: true
        type: LoadBalancer
        annotations:
          coredns.io/hostname: "mariadb-cluster.${SECRET_DOMAIN_INTERNAL}"
          kube-vip.io/loadbalancerIPs: "${MARIADB_LB_ADDR}"
        externalTrafficPolicy: Cluster
        ports:
          http:
            port: 8080
            targetPort: 8080
          mariadb:
            port: 3306
            targetPort: 3306
    serviceMonitor:
      main:
        enabled: true
        endpoints:
          - port: http
            scheme: http
            path: /metrics
            interval: 1m
            scrapeTimeout: 10s
    podSecurityContext:
      runAsUser: 99
      runAsGroup: 99
      fsGroup: 99
      fsGroupChangePolicy: "OnRootMismatch"
    persistence:
      config:
        enabled: true
        type: configMap
        name: mariadb-lb-configmap
        subPath: haproxy.cfg
        mountPath: /usr/local/etc/haproxy/haproxy.cfg
        readOnly: true
