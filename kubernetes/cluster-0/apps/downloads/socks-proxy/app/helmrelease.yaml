---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: socks-proxy
  namespace: downloads
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.0.3
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controller:
      replicas: 2
      strategy: RollingUpdate
      annotations:
        reloader.stakater.com/auto: "true"
    image:
      repository: ghcr.io/drag0n141/docker-socks5
      tag: latest@sha256:bd7cb75c611f87b55603f84bcd861f0f994a01948caa3a28f04e3d4dcfae9bad
    podAnnotations:
      setGateway: "true"
    env:
      TZ: ${TIMEZONE}
      PUID: "1000"
      PGID: "1000"
    service:
      main:
        ports:
          http:
            enabled: false
          socks:
            enabled: true
            port: 1080
        type: LoadBalancer
        annotations:
          coredns.io/hostname: "socks.${SECRET_DOMAIN_INTERNAL}"
          kube-vip.io/loadbalancerIPs: "${SOCKS_LB_ADDR}"
        externalTrafficPolicy: Cluster
    resources:
      requests:
        cpu: 10m
        memory: 128Mi
      limits:
        memory: 128Mi
