---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: &app matchbox
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      matchbox:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-folder:
            image:
              repository: public.ecr.aws/docker/library/busybox
              tag: latest@sha256:650fd573e056b679a5110a70aabeb01e26b76e545ec4b9c70a9523f2dfaf18c6
            command: ["/bin/sh", "-c", "mkdir -p /var/lib/matchbox/{groups,profiles,assets}"]
        containers:
          app:
            image:
              repository: quay.io/poseidon/matchbox
              tag: v0.11.0
            args: ["-address=0.0.0.0:80", "-log-level=debug"]
            env:
              TZ: ${TIMEZONE}
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
            resources:
              requests:
                cpu: 5m
                memory: 256M
              limits:
                memory: 256M
    service:
      app:
        controller: matchbox
        type: LoadBalancer
        annotations:
          io.cilium/lb-ipam-ips: "${MATCHBOX_LB_ADDR}"
        externalTrafficPolicy: Cluster
        ports:
          http:
            port: 80
    persistence:
      matchbox-data:
        storageClass: openebs-hostpath
        size: 5Gi
        accessMode: ReadWriteOnce
        globalMounts:
          - path: /var/lib/matchbox
