---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: filebrowser-oidc
spec:
  secretStoreRefs:
    - name: onepassword-connect
      kind: ClusterSecretStore
  selector:
    secret:
      name: filebrowser-oidc-credentials
  data:
    - match:
        secretKey: client_id
        remoteRef:
          remoteKey: pocket-id
          property: FILEBROWSER_CLIENT_ID
    - match:
        secretKey: client_secret
        remoteRef:
          remoteKey: pocket-id
          property: FILEBROWSER_CLIENT_SECRET
