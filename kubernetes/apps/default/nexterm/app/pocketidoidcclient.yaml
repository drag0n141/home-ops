---
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: nexterm
spec:
  name: Nexterm
  callbackUrls:
    - "https://nexterm.${SECRET_DOMAIN}/api/auth/oidc/callback"
  allowedUserGroups:
    - name: id-admin
      namespace: security
