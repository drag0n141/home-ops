---
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: netbox
spec:
  name: Netbox
  callbackUrls:
    - "https://netbox.${SECRET_DOMAIN_INTERNAL}/oauth/complete/oidc/"
  allowedUserGroups:
    - name: id-admin
      namespace: security
