---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: colota-forwarder
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: colota-forwarder-secret
    template:
      data:
        # App
        API_KEY: "{{ .COLOTA_FORWARDER_API_KEY }}"
        # Connections
        TARGET_1_URL: "https://geopulse.${SECRET_DOMAIN_INTERNAL}/api/colota"
        TARGET_1_AUTH: "{{ .TARGET_1_AUTH }}"
        TARGET_1_TYPE: geopulse
        TARGET_1_FILTER_TID: sj
        TARGET_2_URL: "https://geopulse.${SECRET_DOMAIN_INTERNAL}/api/colota"
        TARGET_2_AUTH: "{{ .TARGET_2_AUTH }}"
        TARGET_2_TYPE: geopulse
        TARGET_2_FILTER_TID: lj
        TARGET_3_URL: "{{ .TARGET_3_URL }}"
        TARGET_3_TYPE: colota
        TARGET_3_BATCH_MODE: latest
  dataFrom:
    - extract:
        key: colota-forwarder
