---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ./helmrelease.yaml
  - ./secret.sops.yaml
configMapGenerator:
  - name: mailrise-configmap
    files:
      - ./config/mailrise.yaml
generatorOptions:
  disableNameSuffixHash: true
