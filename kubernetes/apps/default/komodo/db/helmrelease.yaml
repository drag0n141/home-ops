---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app komodo-db
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      komodo-db:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/library/mongo
              tag: 8.0.12@sha256:412155ba5cdc3674719a516297a7084d33f8a16c54bf32b02b02ccfb6100e8a4
            env:
              TZ: ${TIMEZONE}
            envFrom:
              - secretRef:
                  name: komodo-db-secret
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 512Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          mongo:
            port: 27017
    persistence:
      state:
        existingClaim: *app
        globalMounts:
          - path: /data/db
      tmpfs:
        type: emptyDir
        globalMounts:
          - path: /tmp
