---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gatus-sidecar
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: gatus-sidecar
  values:
    deploymentAnnotations:
      reloader.stakater.com/auto: "true"
    initContainers:
      - name: init-db
        image: ghcr.io/home-operations/postgres-init:18
        envFrom: &envFrom
          - secretRef:
              name: "{{ .Release.Name }}-secret"
    gatus:
      delayStartSeconds: "5"
      env:
        TZ: Europe/Berlin
      envFrom: *envFrom
    sidecar:
      kinds:
        httproute:
          enable: true
          auto: true
        service:
          enable: true
    httpRoute:
      enabled: true
      annotations:
        gatus.home-operations.com/enabled: "false"
      hostnames:
        - "status.${SECRET_DOMAIN_INTERNAL}"
      parentRefs:
        - name: envoy-internal
          namespace: networking
    monitoring:
      serviceMonitor:
        enabled: true
    config:
      existingConfigMap: "{{ .Release.Name }}-configmap"
      items:
        - key: config.yaml
          path: config.yaml
    resources:
      requests:
        cpu: 100m
      limits:
        memory: 512Mi
