---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  labels:
    app.kubernetes.io/instance: home-assistant
    app.kubernetes.io/name: home-assistant
  name: home-assistant
spec:
  endpoints:
    - fqdn:
        hostname: haos.${SECRET_DOMAIN_INT}
        port: 8123
