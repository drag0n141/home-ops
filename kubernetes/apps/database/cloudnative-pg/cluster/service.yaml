---
apiVersion: v1
kind: Service
metadata:
  name: postgres-lb
  namespace: database
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  annotations:
    coredns.io/hostname: postgres.${SECRET_DOMAIN_INTERNAL}
    kube-vip.io/loadbalancerIPs: "${POSTGRES_LB_ADDR}"
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    postgresql: postgres
    role: primary
