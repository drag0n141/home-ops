---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vpn
resources:
  - ./helmrelease.yaml
  - ./secret.sops.yaml
  - ./volsync.yaml
secretGenerator:
  - name: headscale-secret
    files:
      - config.yaml=./config/config.sops.yaml
generatorOptions:
  disableNameSuffixHash: true
