---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: envoy-internal
  annotations:
    external-dns.alpha.kubernetes.io/target: &hostname internal-gateway.${SECRET_DOMAIN_INTERNAL}
    gatus.home-operations.com/endpoint: |-
      alerts:
        - type: gotify
      group: internal
      ui:
        hide-hostname: true
        hide-url: true
spec:
  gatewayClassName: envoy
  infrastructure:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: *hostname
      lbipam.cilium.io/ips: 192.168.222.6
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.${SECRET_DOMAIN}"
      allowedRoutes:
        namespaces:
          from: Same
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.${SECRET_DOMAIN}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: "${SECRET_DOMAIN/./-}-production-tls"
    - name: http-short
      protocol: HTTP
      port: 80
      hostname: "*.${SECRET_DOMAIN_SHORT}"
      allowedRoutes:
        namespaces:
          from: Same
    - name: https-short
      protocol: HTTPS
      port: 443
      hostname: "*.${SECRET_DOMAIN_SHORT}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: "${SECRET_DOMAIN_SHORT/./-}-production-tls"
