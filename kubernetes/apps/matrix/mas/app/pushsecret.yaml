---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: synapse-oidc
spec:
  secretStoreRefs:
    - name: onepassword-connect
      kind: ClusterSecretStore
  selector:
    secret:
      name: synapse-oidc-credentials
  data:
    - match:
        secretKey: client_id
        remoteRef:
          remoteKey: pocket-id
          property: SYNAPSE_CLIENT_ID
    - match:
        secretKey: client_secret
        remoteRef:
          remoteKey: pocket-id
          property: SYNAPSE_CLIENT_SECRET
