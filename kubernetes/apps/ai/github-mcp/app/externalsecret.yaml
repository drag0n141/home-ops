---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github-mcp
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: github-mcp-secret
    template:
      data:
        GH_TOKEN: "{{ .OPENCLAW_GH_TOKEN }}"
  dataFrom:
    - extract:
        key: github
