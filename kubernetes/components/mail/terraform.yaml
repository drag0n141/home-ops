---
apiVersion: infra.contrib.fluxcd.io/v1alpha2
kind: Terraform
metadata:
  name: ${APP}-migadu
spec:
  interval: 12h
  approvePlan: auto
  vars:
    - name: app
      value: ${MAIL_ADDRESS:-${APP}}
    - name: name
      value: ${MAIL_NAME:-${APP}}
    - name: domain
      value: ${SECRET_DOMAIN}
  path: ./terraform/migadu
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  runnerPodTemplate:
    spec:
      envFrom:
        - secretRef:
            name: ${APP}-migadu-token
