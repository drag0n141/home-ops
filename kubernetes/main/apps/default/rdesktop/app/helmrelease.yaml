---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: &app rdesktop
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      rdesktop:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/linuxserver/rdesktop
              tag: latest@sha256:1bc116ddda6337023bf507f88c1f4ef3b6e219da6d0c4081f197f4e5a0b3c771
            env:
              TZ: ${TIMEZONE}
              PUID: 1000
              PGID: 1000
            resources:
              requests:
                cpu: 10m
                memory: 100Mi
              limits:
                memory: 1000Mi
    service:
      app:
        controller: rdesktop
        ports:
          rdp:
            port: 3389
            protocol: TCP
    persistence:
      data:
        existingClaim: rdesktop
        globalMounts:
          - path: /config
