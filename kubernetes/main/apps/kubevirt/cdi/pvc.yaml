---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cdi-volume
  namespace: kubevirt
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-block
