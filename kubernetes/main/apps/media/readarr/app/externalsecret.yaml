---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: readarr
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: readarr-secret
    template:
      engineVersion: v2
      data:
        READARR__POSTGRES_HOST: &dbHost postgres-rw.database.svc.cluster.local
        READARR__POSTGRES_PORT: "5432"
        READARR__POSTGRES_USER: &dbUser "{{ .READARR_POSTGRES_USER }}"
        READARR__POSTGRES_PASSWORD: &dbPass "{{ .READARR_POSTGRES_PASSWORD }}"
        READARR__POSTGRES_MAIN_DB: "{{ .READARR_POSTGRES_MAINDB }}"
        READARR__POSTGRES_LOG_DB: "{{ .READARR_POSTGRES_LOGDB }}"
        READARR__POSTGRES_CACHE_DB: "{{ .READARR_POSTGRES_CACHEDB }}"
        INIT_POSTGRES_DBNAME: "{{ .READARR_POSTGRES_MAINDB }} {{ .READARR_POSTGRES_LOGDB }} {{ .READARR_POSTGRES_CACHEDB }}"
        INIT_POSTGRES_HOST: *dbHost
        INIT_POSTGRES_USER: *dbUser
        INIT_POSTGRES_PASS: *dbPass
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
  dataFrom:
    - extract:
        key: cloudnative-pg
