---
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: checkmk
spec:
  preference:
    kind: VirtualMachinePreference
    name: checkmk
  instancetype:
    kind: VirtualMachineInstancetype
    name: checkmk
  runStrategy: Always
  template:
    metadata:
      labels:
        kubevirt.io/os: linux
    spec:
      hostname: checkmk
      networks:
        - name: eth0
          multus:
            default: true
            networkName: networking/multus-server
      volumes:
        - name: root
          persistentVolumeClaim:
            claimName: checkmk
        - name: disk0
          persistentVolumeClaim:
            claimName: default/debian12
      domain:
        devices:
          disks:
            - name: root
              disk: {}
            - name: disk0
              disk: {}
          interfaces:
            - name: eth0
              bridge: {}
        firmware:
          uuid: "${SECRET_CHECKMK_UUID}"
          bootloader:
            bios:
              useSerial: true
