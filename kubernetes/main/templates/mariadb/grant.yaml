---
apiVersion: mariadb.mmontes.io/v1alpha1
kind: Grant
metadata:
  name: "${APP}"
spec:
  mariaDbRef:
    name: mariadb-galera
  privileges:
    - "ALL"
  database: "${APP}_db"
  table: "*"
  username: "${APP}"
