---
apiVersion: mariadb.mmontes.io/v1alpha1
kind: User
metadata:
  name: "${APP}"
spec:
  mariaDbRef:
    name: mariadb-galera
  passwordSecretKeyRef:
    name: "${APP}-secret"
    key: MARIADB_PASSWORD
  maxUserConnections: 20
