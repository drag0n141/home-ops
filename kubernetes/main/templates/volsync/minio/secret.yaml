---
apiVersion: v1
kind: Secret
metadata:
  name: "${APP}-volsync-minio"
stringData:
    RESTIC_REPOSITORY: '${SECRET_MINIO_RESTIC_REPOSITORY}/${APP}'
    RESTIC_PASSWORD: '${SECRET_MINIO_RESTIC_PASSWORD}'
    AWS_ACCESS_KEY_ID: '${SECRET_MINIO_AWS_ACCESS_KEY_ID}'
    AWS_SECRET_ACCESS_KEY: '${SECRET_MINIO_AWS_SECRET_ACCESS_KEY}'
